cmake_minimum_required (VERSION 2.6)

set(LOCAL_HEADERS ${CMAKE_CURRENT_LIST_DIR}/${INCLUDE_DIR}/)
set(LOCAL_TARGET platform)

# install headers
file(COPY ${LOCAL_HEADERS} DESTINATION ${DEST_INCLUDE_DIR})

include_directories (${DEST_INCLUDE_DIR})

aux_source_directory(${CMAKE_CURRENT_LIST_DIR}/${SOURCE_DIR} PLATFORM_SOURCE_PATH)

add_library(${LOCAL_TARGET} STATIC ${PLATFORM_SOURCE_PATH})
